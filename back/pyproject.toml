[project]
name = "comments-back"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "django==5.2.2",
    "pillow>=12.1.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.7",
]
